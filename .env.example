# Base URL of the backend API (including /api prefix).
# Copy this file to .env (local dev) or .env.production (prod build)
# and set the real value — the app silently falls back to
# http://localhost:5000/api if this is not set.
VITE_API_URL=http://localhost:5000/api
